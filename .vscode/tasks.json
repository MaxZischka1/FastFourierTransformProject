{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Verilog Sim",
            "type": "shell",
            "command": "iverilog -g2012 -o sim.vvp UartRX/*.sv && vvp sim.vvp && gtkwave curTest.vcd",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "detail": "Compiles, runs, and opens GTKWave" 
        }
    ]
}